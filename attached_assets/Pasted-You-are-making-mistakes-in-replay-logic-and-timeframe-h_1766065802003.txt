You are making mistakes in replay logic and timeframe handling.
Read this carefully and implement EXACTLY as described.

🧠 CORE PRINCIPLE (NON-NEGOTIABLE)

The entire chart must be driven by ONE universal timestamp.

currentReplayTimestamp (Unix seconds)


This timestamp is the ONLY source of truth.

No bar index based logic

No reloading data on every click

No random starting points

No future candles

No timeframe-based resets

Everything must be derived from this timestamp.

1️⃣ DATA LOADING RULES (VERY STRICT)
On session creation

Load ALL historical data from scratch → session.fromDate

Render it on the chart

User can scroll and analyze past data

During session usage

Chart must render scratch → currentReplayTimestamp

NEVER jump to random timestamps

NEVER start from middle

❌ Wrong:

Reloading chart from random time

Showing incomplete or future candles

Resetting bars when timeframe changes

✅ Correct:

Chart ALWAYS starts from scratch

Bars are only added as time progresses

2️⃣ DATA MUST NOT RELOAD ON NEXT / PLAY
Current issue

On every Next / Play click, data reloads → causes lag & jumps

Fix (MANDATORY)

Historical data is fetched ONCE

Aggregated timeframe data is cached

On Next, you ONLY:

currentReplayTimestamp += 1 minute


Then re-render using existing data

❌ No API call on every Next
❌ No chart reinitialization
✅ Smooth candle progression

3️⃣ TIMEFRAME SWITCHING LOGIC (MOST IMPORTANT)

Timeframe switch MUST NOT change time.

Only candle grouping changes.

Example (IMPLEMENT EXACTLY THIS BEHAVIOR)

Assume:

currentReplayTimestamp = 12:22

🟢 On 1-minute timeframe

Last candle: 12:22

🟢 Switch to 1H timeframe

Last candle must be:

12:00 → 12:22 (PARTIAL candle)


❌ DO NOT jump to 13:00
❌ DO NOT show full 12:00–13:00

🟢 Click NEXT (1H)

Time jumps to:

13:00


Next clicks:

14:00 → 15:00 → ...

🟢 Switch to 5-minute timeframe (current time = 14:00)

Last candle:

14:00


Click NEXT:

14:05 → 14:10 → 14:15 → ...

🟢 Switch to 4-hour timeframe (current time = 14:15)

Last candle:

12:00 → 14:15 (PARTIAL 4H candle)


Click NEXT:

16:00 → 20:00 → ...

🔑 RULE TO FOLLOW FOR ALL TIMEFRAMES
bucketStart = floor(currentReplayTimestamp / timeframeSeconds) * timeframeSeconds


Last candle = bucketStart → currentReplayTimestamp

Always PARTIAL until bucket completes

No future data

4️⃣ BACKEND DATA CONTRACT (DO NOT CHANGE)

Backend already works correctly and returns:

{
  "s": "ok",
  "t": [...],
  "o": [...],
  "h": [...],
  "l": [...],
  "c": [...],
  "v": [...]
}


Data is from scratch → toTimestamp

Last candle is partial

Backend aggregation is correct

❌ Do NOT modify backend logic
❌ Do NOT re-aggregate in frontend

5️⃣ FRONTEND RESPONSIBILITIES
Store once:
allBarsByTimeframe: Map<resolution, bars[]>
currentReplayTimestamp: number

On Next / Play:
currentReplayTimestamp += baseStepSeconds
renderBarsUpTo(currentReplayTimestamp)

On timeframe change:
DO NOT change timestamp
ONLY change candle grouping

6️⃣ WHAT I EXPECT AFTER FIX

Smooth replay

No data reload on Next

Time NEVER jumps incorrectly

Multi-timeframe analysis behaves like FX Replay

Partial candles handled correctly

Chart always starts from scratch

❌ IF ANY OF THIS HAPPENS, IT IS WRONG

Chart resets on timeframe switch

Candles jump ahead in time

Last candle shows future OHLC

Data reloads on Next

Bar index is used as source of truth

✅ FINAL NOTE

If unsure:

Timestamp > index

Partial candle > full future candle

Cached data > refetch

Smoothness > brute-force reload

Implement exactly this logic.